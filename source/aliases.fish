# Aliases
alias :q="exit"
alias xc="exit"

# For some reason `evil-yank` and `evil-paste` are relying on pbcopy and pbpaste which are related to mac
# TODO: Fix this by removing the dependency on pbcopy and pbpaste
#alias pbcopy='xclip -selection clipboard'
#alias pbpaste='xclip -selection clipboard -o'

alias v="nvim"
